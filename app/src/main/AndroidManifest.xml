<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sompod44soft.rentgiant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Progressing_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".RentGiant_Activity"
            android:label="@string/title_activity_rent_giant_"
            android:theme="@style/AppTheme" />
        <activity android:name=".Itemshow_Activity" />
        <activity android:name=".ItemDetails_showing_Activity" />
        <activity android:name=".Register_From" />
        <activity android:name=".Send_Feedback"></activity>
    </application>

</manifest>